# Architecture Decisions

Architecture decision records use the template in this directory and progress
through `proposed`, `accepted`, `superseded`, or `rejected` states.

File names use `NNNN-short-title.md`. Accepted decisions are immutable except
for clarifications; changed direction requires a superseding record.
